import { Grid } from "@/components/simulation/Grid";
import Particle2, { Params } from "@/components/simulation/materials/Particle2";

export class Behaviour2 {
  update(particle: Particle2, grid: Grid, params: Params) {
    void particle;
    void grid;
    void params;
  }
}
